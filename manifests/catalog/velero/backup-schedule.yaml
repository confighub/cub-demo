apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: cluster-nightly
  namespace: velero
spec:
  schedule: 0 3 * * *
  template:
    includedNamespaces: ["*"]
    excludedNamespaces: [kube-system]
    ttl: 720h
