apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-gateway-api
  namespace: payment-gateway
  labels:
    app.kubernetes.io/name: payment-gateway
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: payment-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-gateway
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-gateway
        app.kubernetes.io/component: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8443"
    spec:
      containers:
        - name: api
          image: registry.{{.Scenario.Domain}}/payment-gateway/api:{{.Values.version}}
          ports:
            - name: http
              containerPort: 8443
          env:
            - name: LOG_LEVEL
              value: debug
            - name: AWS_REGION
              value: us-east-1
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: 7.0.4-conn
                  key: endpoint
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-api
  namespace: payment-gateway
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: payment-gateway
    app.kubernetes.io/component: api
